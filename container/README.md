# P.E.T.E.R.-container

Running `./000-run-all-bash-scripts.run` will create a systemd-nspawn container
machine as specified in `machine.variables`.
Each script can also be executed manually to update the configuration of the
container.
